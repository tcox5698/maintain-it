<%= form_with(model: site_member, local: true) do |form| %>
  <% if site_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site_member.errors.count, "error") %> prohibited this site_member from being saved:</h2>

      <ul>
      <% site_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nick_name %>
    <%= form.text_field :nick_name, id: :site_member_nick_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :site_member_user_id %>
  </div>

  <div class="field">
    <%= form.label :site_id %>
    <%= form.text_field :site_id, id: :site_member_site_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
